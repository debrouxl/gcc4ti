[Main]
Name=peekIO_w
Type=Function
Subtype=Macro
Header Files=peekpoke.h
Definition=#define peekIO_w(port) (*((volatile unsigned short*)(long)(port)))

[Description]
Reads a word from an I/O port.

[Explanation]
peekIO_w is like <A HREF="$$LINK(peekIO)">peekIO</A>, but reads a word from the I/O port instead of
a byte.
